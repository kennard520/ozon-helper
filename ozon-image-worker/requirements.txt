# Ozon 出图 worker —— 运行依赖
# 是 ozon-listing-webui/requirements.txt 的子集（仅出图链路所需），版本区间保持一致。

pika>=1.3                     # RabbitMQ 客户端（消费出图任务）
PyMySQL>=1.1                  # MySQL 后端
oss2>=2.18                    # 阿里云 OSS 图片直链
openai>=1.30                  # AI 出图 / 图片理解
