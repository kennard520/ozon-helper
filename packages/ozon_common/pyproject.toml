[project]
name = "ozon-common"
version = "0.1.0"
description = "Ozon 上品助手共享内核:db/oss/mq/出图/数据访问"
requires-python = ">=3.10"
dependencies = [
    "PyMySQL>=1.1",
    "oss2>=2.18",
    "pika>=1.3",
    "openai>=1.30",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
