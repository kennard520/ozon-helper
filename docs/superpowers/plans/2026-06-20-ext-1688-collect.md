# 插件 1688 采集 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `ozon-seller-helper-ext` 扩展中实现 1688 商品详情页的全量采集（含所有 SKU 变体、详情图富文本、视频），建成 Ozon 草稿复用现有发布管线。

**Architecture:** 新增纯函数解析器 `common/parse-1688.js`（读 `window.context.result.data` / `window.offer_details.content` / DOM 属性，产出后端 `collectParsed` 所需的 data 结构 + 富文本 + 全量 SKU 变体）；`collect-flow.js` 新增编排函数 `collect1688AndEdit`（CNY 不换汇，每个 SKU 建一草稿，秒回 + 媒体异步传 OSS）；`panel.js` 把 1688 路由到真实采集；`manifest.json` 注册脚本。**1688 走 WB 模式：解析产出直接 `collectParsed`，不经 `buildCollectData`**（buildCollectData 读 `characteristics` 且丢弃 `source_raw`，不适用）。解析逻辑全做成接受参数的纯函数，在 vitest（node 环境）单测；编排/接线手动加载插件验证。

**Tech Stack:** 原生 JS（UMD 模块，content script 全局 + vitest import）、vitest 2.x（node 环境，无 DOM）、Chrome MV3 扩展。

**参考设计：** `docs/superpowers/specs/2026-06-20-ext-1688-collect-design.md`

---

## 文件结构

- **Create** `ozon-seller-helper-ext/common/parse-1688.js` — UMD 模块 `OzonHelperParse1688`，纯函数解析器。职责：把 1688 页面数据结构翻译成后端 `collectParsed` 直收的 data 结构（含 `attributes` 名值对、`rich_content_json`、`source_raw`；对齐 WB `parseCard` 的字段约定）。
- **Create** `ozon-seller-helper-ext/tests/parse-1688.test.js` — 解析器单测。
- **Modify** `ozon-seller-helper-ext/common/collect-flow.js` — 新增 `collect1688AndEdit()`，加入 `OzonHelperCollect` 导出。
- **Modify** `ozon-seller-helper-ext/content/panel.js` — `onEditCurrent()` / `update()` 中 `site==='1688'` 路由到真实采集。
- **Modify** `ozon-seller-helper-ext/manifest.json` — content_scripts 注册 `common/parse-1688.js`。

## 关键数据形态（实测，用于测试 fixture）

- `data.productTitle.fields.title` = 标题（string）
- `data.mainPrice.fields.priceModel.originalPriceDisplay` = "18.79-141.63"
- `data.gallery.fields.offerImgList` = string[]（图 URL）
- `data.gallery.fields.video` = `{ coverUrl, videoId, state, title, videoUrl }`（videoUrl 是 mp4 直链）
- `data.Root.fields.dataJson.skuModel.skuProps` = `[{ fid, prop, value:[{ name, imageUrl }] }]`
- `data.Root.fields.dataJson.skuModel.skuInfoMap` = `{ "<specAttrs>": { specId, specAttrs, price, discountPrice, canBookCount, skuId } }`
- `data.productPackInfo.fields.pieceWeightScale.pieceWeightScaleInfo` = `[{ sku1, skuId, weight(克), length, width, height(mm), volume }]`
- 属性表 DOM：`div.module-od-product-attributes` 内含 `<tr class="ant-descriptions-row"><th class="ant-descriptions-item-label"><span>名</span></th><td class="ant-descriptions-item-content"><span><span class="field-value">值</span></span></td>…</tr>`
- 详情：`window.offer_details.content` = HTML 字符串，含 `<img src="https://cbu01.alicdn.com/...jpg">`

---

## Task 1: parse-1688.js 骨架 + extractOfferId

**Files:**
- Create: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

- [ ] **Step 1: Write the failing test**

```js
// ozon-seller-helper-ext/tests/parse-1688.test.js
import { describe, it, expect } from 'vitest'
import OzonHelperParse1688 from '../common/parse-1688.js'

const { extractOfferId } = OzonHelperParse1688

describe('extractOfferId', () => {
  it('从 detail.1688.com/offer/<id>.html 提取 offerId', () => {
    expect(extractOfferId('https://detail.1688.com/offer/795554901999.html?spm=a360q')).toBe('795554901999')
  })
  it('无 offer 段 → 空串', () => {
    expect(extractOfferId('https://www.1688.com/')).toBe('')
    expect(extractOfferId('')).toBe('')
    expect(extractOfferId(null)).toBe('')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— 找不到模块 `../common/parse-1688.js`。

- [ ] **Step 3: Write minimal implementation**

```js
// ozon-seller-helper-ext/common/parse-1688.js
// 1688 商品页纯函数（UMD：content script 全局 OzonHelperParse1688 + vitest import）
// 数据源：window.context.result.data（主数据）/ window.offer_details.content（详情HTML）/ DOM 属性表
;(function (root, factory) {
  const api = factory()
  if (typeof module === 'object' && module.exports) module.exports = api
  root.OzonHelperParse1688 = api
})(typeof globalThis !== 'undefined' ? globalThis : self, function () {
  // 从 detail.1688.com/offer/<id>.html 提取 offerId（作 variant_group / source_offer_id）
  function extractOfferId(url) {
    const m = String(url || '').match(/\/offer\/(\d+)\.html/)
    return m ? m[1] : ''
  }

  return { extractOfferId }
})
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS（2 个用例）。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 骨架 + extractOfferId"
```

---

## Task 2: parseDetailImages（详情 HTML → 图 URL 列表）

**Files:**
- Modify: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

- [ ] **Step 1: Write the failing test**

```js
// 追加到 tests/parse-1688.test.js
const { parseDetailImages } = OzonHelperParse1688

describe('parseDetailImages', () => {
  const html = '<div><p>详情</p>' +
    '<img src="https://cbu01.alicdn.com/img/ibank/a.jpg?x=1">' +
    "<img src='https://cbu01.alicdn.com/img/ibank/b.png'>" +
    '<img src="https://cbu01.alicdn.com/img/ibank/a.jpg?y=2">' +  // 与第1张去 query 后重复
    '<img src="data:image/gif;base64,R0lGOD">' +                  // 非 http，剔除
    '</div>'
  it('提取 http(s) 图、去 query、按序去重', () => {
    const imgs = parseDetailImages(html)
    expect(imgs).toEqual([
      'https://cbu01.alicdn.com/img/ibank/a.jpg',
      'https://cbu01.alicdn.com/img/ibank/b.png'
    ])
  })
  it('空/非字符串 → 空数组', () => {
    expect(parseDetailImages('')).toEqual([])
    expect(parseDetailImages(null)).toEqual([])
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— `parseDetailImages is not a function`。

- [ ] **Step 3: Write minimal implementation**

在 `parse-1688.js` 的 factory 内、`return` 之前加入，并把函数加进 return：

```js
  // 从详情 HTML 提取 <img src>，只留 http(s)，去 query，按出现顺序去重
  function parseDetailImages(html) {
    if (typeof html !== 'string' || !html) return []
    const out = []
    const seen = new Set()
    const re = /<img\b[^>]*?\bsrc\s*=\s*["']([^"']+)["']/gi
    let m
    while ((m = re.exec(html))) {
      let u = (m[1] || '').trim()
      if (!/^https?:\/\//i.test(u)) continue
      u = u.split('?')[0]
      if (!seen.has(u)) { seen.add(u); out.push(u) }
    }
    return out
  }
```

return 改为：`return { extractOfferId, parseDetailImages }`

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 parseDetailImages"
```

---

## Task 3: buildRichContent（图列表 → Ozon 富文本 JSON）

**Files:**
- Modify: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

> 富文本结构须满足两点：①顶层有 `content` 数组；②每张图是 `{ img: { src, srcMobile } }` 节点，以便被现有 `media-upload.js` 的 `_collectRich` 收集去 OSS。本测试同时 import `collectMediaUrls` 做交叉验证。widgetName/type 用 Ozon 通用的 `raShowcase`/`billboard`，真机验证后如需可微调常量。

- [ ] **Step 1: Write the failing test**

```js
// import 须放文件顶部（与已有的 import 同级，不能写在 describe 中间）：
import OzonHelperMedia from '../common/media-upload.js'
// 其余 const/describe 追加到 tests/parse-1688.test.js
const { buildRichContent } = OzonHelperParse1688

describe('buildRichContent', () => {
  it('每张图一个 raShowcase block，结构含 content 数组', () => {
    const rcj = buildRichContent(['https://x/a.jpg', 'https://x/b.jpg'])
    expect(Array.isArray(rcj.content)).toBe(true)
    expect(rcj.content.length).toBe(2)
    expect(rcj.content[0].blocks[0].img.src).toBe('https://x/a.jpg')
    expect(rcj.content[0].blocks[0].img.srcMobile).toBe('https://x/a.jpg')
  })
  it('图能被现有媒体管线 collectMediaUrls 收集（保证发布时走 OSS）', () => {
    const rcj = buildRichContent(['https://x/a.jpg', 'https://x/b.jpg'])
    const urls = OzonHelperMedia.collectMediaUrls({ images: [], rich_content_json: rcj })
    expect(urls.sort()).toEqual(['https://x/a.jpg', 'https://x/b.jpg'])
  })
  it('空列表 → null（无富文本时不应产生空属性）', () => {
    expect(buildRichContent([])).toBe(null)
    expect(buildRichContent(null)).toBe(null)
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— `buildRichContent is not a function`。

- [ ] **Step 3: Write minimal implementation**

```js
  // 详情图列表 → Ozon richAnnotationJson（A+富文本）；每张图一个 raShowcase 块。
  // 内嵌图节点为 {img:{src,srcMobile}}，与 media-upload._collectRich 对齐 → 发布前自动传 OSS。
  function buildRichContent(imgUrls) {
    const list = Array.isArray(imgUrls) ? imgUrls.filter((u) => typeof u === 'string' && u) : []
    if (!list.length) return null
    return {
      content: list.map((u) => ({
        widgetName: 'raShowcase',
        type: 'billboard',
        blocks: [{ img: { src: u, srcMobile: u, alt: '' } }]
      })),
      version: 0.3
    }
  }
```

return 加入 `buildRichContent`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 buildRichContent（详情图→A+富文本）"
```

---

## Task 4: parseAttributes（ant-descriptions HTML → 名值对）

**Files:**
- Modify: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

> content script 里传 `document.querySelector('.module-od-product-attributes')?.outerHTML`。解析基于 Ant Design Descriptions 结构：label 在 `ant-descriptions-item-label` 的 `<span>`，值在 `ant-descriptions-item-content` 内的 `.field-value`。一个 `<tr>` 可含多组。用全局正则按出现顺序提取 label 和 value 并配对。

- [ ] **Step 1: Write the failing test**

```js
// 追加到 tests/parse-1688.test.js
const { parseAttributes } = OzonHelperParse1688

describe('parseAttributes', () => {
  const html = '<div class="module-od-product-attributes"><table><tbody>' +
    '<tr class="ant-descriptions-row">' +
      '<th class="ant-descriptions-item-label"><span>产地</span></th>' +
      '<td class="ant-descriptions-item-content"><span><span class="field-value">江苏</span></span></td>' +
      '<th class="ant-descriptions-item-label"><span>是否进口</span></th>' +
      '<td class="ant-descriptions-item-content"><span><span class="field-value">否</span></span></td>' +
    '</tr>' +
    '<tr class="ant-descriptions-row">' +
      '<th class="ant-descriptions-item-label"><span>金属材质</span></th>' +
      '<td class="ant-descriptions-item-content"><span><span class="field-value">白铁皮</span></span></td>' +
    '</tr>' +
    '</tbody></table></div>'
  it('提取 name/value 名值对', () => {
    expect(parseAttributes(html)).toEqual([
      { name: '产地', value: '江苏' },
      { name: '是否进口', value: '否' },
      { name: '金属材质', value: '白铁皮' }
    ])
  })
  it('空/无匹配 → 空数组', () => {
    expect(parseAttributes('')).toEqual([])
    expect(parseAttributes('<div>无属性</div>')).toEqual([])
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— `parseAttributes is not a function`。

- [ ] **Step 3: Write minimal implementation**

```js
  // 去标签取纯文本
  function _text(s) {
    return String(s == null ? '' : s).replace(/<[^>]+>/g, '').replace(/&nbsp;/gi, ' ').trim()
  }
  // ant-descriptions HTML → [{name,value}]。label/content 在 HTML 里成对交替出现，按序配对。
  function parseAttributes(html) {
    if (typeof html !== 'string' || !html) return []
    const tokens = []
    const re = /ant-descriptions-item-(label|content)[^>]*>([\s\S]*?)<\/(?:th|td)>/gi
    let m
    while ((m = re.exec(html))) tokens.push({ kind: m[1], text: _text(m[2]) })
    const out = []
    for (let i = 0; i < tokens.length - 1; i++) {
      if (tokens[i].kind === 'label' && tokens[i + 1].kind === 'content') {
        const name = tokens[i].text
        const value = tokens[i + 1].text
        if (name && value) out.push({ name, value })
        i++ // 跳过已配对的 content
      }
    }
    return out
  }
```

return 加入 `parseAttributes`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 parseAttributes（ant-descriptions名值对）"
```

---

## Task 5: parse1688Base（主数据：标题/价格/主图/视频/属性/富文本/类目）

**Files:**
- Modify: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

> `parse1688Base(data, detailHtml, attrHtml, url)`：`data` = `window.context.result.data`。产出直接发后端的 data 对象（对齐 WB `parseCard` 字段：`attributes`/`source_raw`/`rich_content_json`），仅"全商品共用"字段（变体专属的价/克重/变体图在 Task 6 展开）。所有取值防御性兜底，路径缺失不抛错。

- [ ] **Step 1: Write the failing test**

```js
// 追加到 tests/parse-1688.test.js
const { parse1688Base } = OzonHelperParse1688

function mockData() {
  return {
    productTitle: { fields: { title: '加厚铁油桶汽油桶' } },
    mainPrice: { fields: { priceModel: { originalPriceDisplay: '18.79-141.63' } } },
    gallery: { fields: {
      offerImgList: ['https://cbu01.alicdn.com/m1.jpg', 'https://cbu01.alicdn.com/m2.jpg'],
      video: { coverUrl: 'https://img.alicdn.com/cover.jpg', videoId: 428131987474,
               videoUrl: 'https://cloud.video.taobao.com/play/x/428131987474.mp4' }
    } },
    productPackInfo: { fields: { pieceWeightScale: { pieceWeightScaleInfo: [] } } },
    Root: { fields: { dataJson: { skuModel: { skuProps: [], skuInfoMap: {} }, offerBaseInfo: {} } } }
  }
}
const DETAIL = '<div><img src="https://cbu01.alicdn.com/d1.jpg"><img src="https://cbu01.alicdn.com/d2.jpg"></div>'

describe('parse1688Base', () => {
  it('标题/主图/视频/富文本/source_raw', () => {
    const b = parse1688Base(mockData(), DETAIL, '', 'https://detail.1688.com/offer/795554901999.html')
    expect(b.source_platform).toBe('1688')
    expect(b.title).toBe('加厚铁油桶汽油桶')
    expect(b.images).toEqual(['https://cbu01.alicdn.com/m1.jpg', 'https://cbu01.alicdn.com/m2.jpg'])
    expect(b.video_url).toBe('https://cloud.video.taobao.com/play/x/428131987474.mp4')
    expect(b.rich_content_json.content.length).toBe(2)        // 2 张详情图
    expect(b.source_raw.offer_id).toBe('795554901999')
    expect(b.source_raw.price_display).toBe('18.79-141.63')
  })
  it('缺字段不抛错，降级为空', () => {
    const b = parse1688Base({}, '', '', '')
    expect(b.title).toBe('')
    expect(b.images).toEqual([])
    expect(b.video_url).toBe('')
    expect(b.rich_content_json).toBe(null)
  })
}) 
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— `parse1688Base is not a function`。

- [ ] **Step 3: Write minimal implementation**

```js
  // 安全取嵌套字段
  function _get(obj, path) {
    let cur = obj
    for (const k of path) {
      if (cur == null) return undefined
      cur = cur[k]
    }
    return cur
  }

  // 主数据：全商品共用字段（变体专属的价/克重/变体图见 expandSkus）
  // data = window.context.result.data；detailHtml = window.offer_details.content；attrHtml = 属性容器 outerHTML
  function parse1688Base(data, detailHtml, attrHtml, url) {
    data = data || {}
    const title = _get(data, ['productTitle', 'fields', 'title']) || ''
    const images = _get(data, ['gallery', 'fields', 'offerImgList'])
    const imgs = Array.isArray(images) ? images.filter((u) => typeof u === 'string' && u) : []
    const video = _get(data, ['gallery', 'fields', 'video']) || {}
    const priceDisplay = _get(data, ['mainPrice', 'fields', 'priceModel', 'originalPriceDisplay']) || ''
    return {
      source_platform: '1688',
      title: title,
      description: '',                       // 留空，webui AI 生成俄语
      images: imgs,
      detail_images: [],
      rich_content_json: buildRichContent(parseDetailImages(detailHtml)),
      video_url: typeof video.videoUrl === 'string' ? video.videoUrl : '',
      attributes: parseAttributes(attrHtml),
      price: '', old_price: '',              // 变体展开时填
      weight_g: null, length_mm: null, width_mm: null, height_mm: null,  // 变体展开时按 skuId 填
      category_path: '',
      source_raw: {
        offer_id: extractOfferId(url),
        price_display: priceDisplay,
        video_cover: typeof video.coverUrl === 'string' ? video.coverUrl : '',
        attributes: parseAttributes(attrHtml)
      }
    }
  }
```

return 加入 `parse1688Base`（`_get` 内部用，不必导出）。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 parse1688Base（主数据）"
```

---

## Task 6: expandSkus（全量 SKU 展开 + 克重尺寸/变体图匹配）

**Files:**
- Modify: `ozon-seller-helper-ext/common/parse-1688.js`
- Test: `ozon-seller-helper-ext/tests/parse-1688.test.js`

> `expandSkus(data, base)` → 变体草稿数组。每条 = base 浅拷贝 + 覆盖 `price`（该 SKU CNY 数字字符串）、克重尺寸（按 skuId 在 pieceWeightScaleInfo 匹配；weight 已是克、长宽高已是 mm，不换算）、`images`（变体图置顶 + 主图，去重）、`variant_label`（specAttrs）、`source_raw`（追加 sku_id/spec_id/stock）。无 SKU（skuInfoMap 空）→ 返回单条，价取 priceModel 区间最低。变体图：单维度时 specAttrs 直接等于 skuProps[0].value[].name。

- [ ] **Step 1: Write the failing test**

```js
// 追加到 tests/parse-1688.test.js
const { expandSkus } = OzonHelperParse1688

function mockDataWithSku() {
  const d = mockData()
  d.Root.fields.dataJson.skuModel = {
    skuProps: [{ fid: 1234, prop: '规格', value: [
      { name: '摩托车专用款3升铝盖+油管', imageUrl: 'https://cbu01.alicdn.com/v3l.jpg' },
      { name: '标准加厚铁盖立式10L+油管', imageUrl: 'https://cbu01.alicdn.com/v10l.jpg' }
    ] }],
    skuInfoMap: {
      '摩托车专用款3升铝盖+油管': { specId: 's1', specAttrs: '摩托车专用款3升铝盖+油管', price: '18.79', discountPrice: '18.79', canBookCount: 9973, skuId: 5595723402563 },
      '标准加厚铁盖立式10L+油管': { specId: 's2', specAttrs: '标准加厚铁盖立式10L+油管', price: '35.50', discountPrice: '35.50', canBookCount: 100, skuId: 5595723402566 }
    }
  }
  d.productPackInfo.fields.pieceWeightScale.pieceWeightScaleInfo = [
    { sku1: '摩托车专用款3升铝盖+油管', skuId: 5595723402563, weight: 940, length: 310, width: 180, height: 110 },
    { sku1: '标准加厚铁盖立式10L+油管', skuId: 5595723402566, weight: 1600, length: 295, width: 130, height: 370 }
  ]
  return d
}

describe('expandSkus', () => {
  it('每个 SKU 一条，含价/库存/克重尺寸/变体图', () => {
    const data = mockDataWithSku()
    const base = parse1688Base(data, DETAIL, '', 'https://detail.1688.com/offer/795554901999.html')
    const list = expandSkus(data, base)
    expect(list.length).toBe(2)
    const a = list[0]
    expect(a.variant_label).toBe('摩托车专用款3升铝盖+油管')
    expect(a.price).toBe('18.79')
    expect(a.weight_g).toBe(940)
    expect(a.length_mm).toBe(310)
    expect(a.width_mm).toBe(180)
    expect(a.height_mm).toBe(110)
    expect(a.images[0]).toBe('https://cbu01.alicdn.com/v3l.jpg')   // 变体图置顶
    expect(a.images).toContain('https://cbu01.alicdn.com/m1.jpg')  // 含主图
    expect(a.source_raw.sku_id).toBe(5595723402563)
    expect(a.source_raw.stock).toBe(9973)
    expect(a.rich_content_json.content.length).toBe(2)             // 富文本共用
  })
  it('无 SKU → 单条，价取区间最低', () => {
    const data = mockData()  // skuInfoMap 空
    const base = parse1688Base(data, DETAIL, '', 'https://detail.1688.com/offer/795554901999.html')
    const list = expandSkus(data, base)
    expect(list.length).toBe(1)
    expect(list[0].price).toBe('18.79')   // 来自 originalPriceDisplay "18.79-141.63" 最低
  })
}) 
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: FAIL —— `expandSkus is not a function`。

- [ ] **Step 3: Write minimal implementation**

```js
  // 按 skuId 在 pieceWeightScaleInfo 找克重尺寸（weight 已是克、长宽高已是 mm）
  function _packBySkuId(packInfo, skuId) {
    for (const p of packInfo || []) {
      if (p && p.skuId === skuId) {
        return {
          weight_g: typeof p.weight === 'number' ? p.weight : null,
          length_mm: typeof p.length === 'number' ? p.length : null,
          width_mm: typeof p.width === 'number' ? p.width : null,
          height_mm: typeof p.height === 'number' ? p.height : null
        }
      }
    }
    return { weight_g: null, length_mm: null, width_mm: null, height_mm: null }
  }

  // 在 skuProps 维度值里按规格名找变体图（单维度：specAttrs === value.name）
  function _variantImage(skuProps, specAttrs) {
    for (const prop of skuProps || []) {
      for (const v of (prop && prop.value) || []) {
        if (v && v.name && specAttrs.indexOf(v.name) >= 0 && v.imageUrl) return v.imageUrl
      }
    }
    return ''
  }

  // 区间最低价（"18.79-141.63" → "18.79"）
  function _lowestFromDisplay(disp) {
    const nums = String(disp || '').match(/\d+(?:\.\d+)?/g)
    return nums && nums.length ? nums[0] : ''
  }

  // 全量 SKU 展开 → 变体草稿数组
  function expandSkus(data, base) {
    data = data || {}
    base = base || {}
    const skuModel = _get(data, ['Root', 'fields', 'dataJson', 'skuModel']) || {}
    const infoMap = skuModel.skuInfoMap || {}
    const skuProps = skuModel.skuProps || []
    const packInfo = _get(data, ['productPackInfo', 'fields', 'pieceWeightScale', 'pieceWeightScaleInfo']) || []
    const keys = Object.keys(infoMap)
    const baseImgs = Array.isArray(base.images) ? base.images : []

    if (!keys.length) {
      // 无 SKU：单条，价取区间最低
      const price = _lowestFromDisplay(_get(base, ['source_raw', 'price_display']))
      return [Object.assign({}, base, { price: price })]
    }

    return keys.map((k) => {
      const sku = infoMap[k] || {}
      const dims = _packBySkuId(packInfo, sku.skuId)
      const vimg = _variantImage(skuProps, sku.specAttrs || k)
      const images = []
      const push = (u) => { if (u && images.indexOf(u) < 0) images.push(u) }
      push(vimg)
      baseImgs.forEach(push)
      return Object.assign({}, base, {
        price: sku.price != null ? String(sku.price) : '',
        variant_label: sku.specAttrs || k,
        weight_g: dims.weight_g,
        length_mm: dims.length_mm,
        width_mm: dims.width_mm,
        height_mm: dims.height_mm,
        images: images,
        source_raw: Object.assign({}, base.source_raw, {
          sku_id: sku.skuId, spec_id: sku.specId, spec_attrs: sku.specAttrs, stock: sku.canBookCount
        })
      })
    })
  }
```

return 加入 `expandSkus`。

- [ ] **Step 4: Run test to verify it passes**

Run: `cd ozon-seller-helper-ext && npx vitest run tests/parse-1688.test.js`
Expected: PASS（全文件用例全绿）。

- [ ] **Step 5: Commit**

```bash
git add ozon-seller-helper-ext/common/parse-1688.js ozon-seller-helper-ext/tests/parse-1688.test.js
git commit -m "feat(ext): parse-1688 expandSkus（全量SKU+克重尺寸+变体图）"
```

---

## Task 7: collect-flow.js 新增 collect1688AndEdit

**Files:**
- Modify: `ozon-seller-helper-ext/common/collect-flow.js`

> 编排函数，依赖 content script 全局：`OzonHelperParse1688` / `OzonHelperBridge`。无单测（依赖 bridge/window），由 Task 8 手动加载插件验证。读全局数据 → 展开 SKU → 每个 SKU 直接 `collectParsed`（像 WB 那样直发解析产出，保留 `source_raw`/富文本，**不经 `buildCollectData`**）。1688 是 CNY，不取汇率不换算。

- [ ] **Step 1: 在 collect-flow.js 实现 collect1688AndEdit**

在 `collect-flow.js` 中，`collectWbAndEdit` 之后、`collectAllVariants` 之前插入：

```js
  // 1688：全插件采集——读页面全局数据(window.context/offer_details) + DOM 属性 → 全量 SKU 各建一草稿。
  // 1688 是人民币站，不换汇；图/视频/富文本图走后台异步传 OSS。
  async function collect1688AndEdit(url, onStatus) {
    if (typeof OzonHelperParse1688 === 'undefined' || typeof OzonHelperBridge === 'undefined') return
    if (onStatus) onStatus('采集中…', true)
    const data = (typeof window !== 'undefined' && window.context && window.context.result && window.context.result.data) || null
    if (!data) { if (onStatus) onStatus('请等商品详情加载完再采集（未读到页面数据）', false); return }
    const detailHtml = (typeof window !== 'undefined' && window.offer_details && window.offer_details.content) || ''
    const attrEl = (typeof document !== 'undefined') ? document.querySelector('.module-od-product-attributes') : null
    const attrHtml = attrEl ? attrEl.outerHTML : ''
    const base = OzonHelperParse1688.parse1688Base(data, detailHtml, attrHtml, url)
    if (!base.title) { if (onStatus) onStatus('未识别到 1688 商品（标题为空）', false); return }
    const group = OzonHelperParse1688.extractOfferId(url)
    const variants = OzonHelperParse1688.expandSkus(data, base)
    let firstDraftId = null
    let autoPublish = false
    let ok = 0
    for (let i = 0; i < variants.length; i++) {
      if (onStatus) onStatus('采集 ' + (i + 1) + '/' + variants.length + '…', true)
      const cd = variants[i]   // 像 WB 一样直发解析产出（不经 buildCollectData，保留 source_raw/rich_content_json）
      if (group) cd.variant_group = group
      try {
        const r = await OzonHelperBridge.bgCall('collectParsed', { url, data: cd })
        if (r && r.ok) {
          ok++
          if (firstDraftId == null) {
            const created = r.data && r.data.created
            firstDraftId = Array.isArray(created) && created[0] ? created[0].id : null
            autoPublish = !!(r.data && r.data.auto_publish)
          }
        }
      } catch (e) { /* 单个失败跳过 */ }
    }
    if (!ok) { if (onStatus) onStatus('采集失败：连不上后端服务器，请检查网络或联系管理员', false); return }
    OzonHelperBridge.bgCall('rehostPending')   // 踢后台补传图/视频到 OSS
    if (onStatus) onStatus('已采集 ' + ok + '/' + variants.length + ' ✓', true)
    if (!autoPublish) OzonHelperBridge.bgCall('openEditor', { draftId: firstDraftId })
  }
```

并把导出行改为：

```js
  root.OzonHelperCollect = { collectAndEdit, collectAllVariants, collectWbAndEdit, collect1688AndEdit }
```

- [ ] **Step 2: 校验语法（无独立单测）**

Run: `cd ozon-seller-helper-ext && node -e "require('./common/parse-1688.js'); console.log('parse-1688 loads')"`
Expected: 打印 `parse-1688 loads`（确认依赖模块语法正常；collect-flow.js 依赖浏览器全局，不在 node 直接 require）。
Run: `cd ozon-seller-helper-ext && npx vitest run`
Expected: 全部 PASS（确认未破坏既有测试）。

- [ ] **Step 3: Commit**

```bash
git add ozon-seller-helper-ext/common/collect-flow.js
git commit -m "feat(ext): collect-flow 新增 collect1688AndEdit（全量SKU采集）"
```

---

## Task 8: panel.js 路由 + manifest.json 注册（接线 + 手动验证）

**Files:**
- Modify: `ozon-seller-helper-ext/content/panel.js`
- Modify: `ozon-seller-helper-ext/manifest.json`

- [ ] **Step 1: manifest.json 注册解析器**

把 `manifest.json` content_scripts 的 `js` 数组中 `"common/wb.js"` 与 `"common/media-upload.js"` 之间（或 `"common/collect-flow.js"` 之前任意位置，须早于 collect-flow）加入 `"common/parse-1688.js"`。改后该数组为：

```json
"js": ["common/site.js", "common/parse-other-offers.js", "common/bridge.js", "common/product-parse.js", "common/wb.js", "common/parse-1688.js", "common/media-upload.js", "common/collect-flow.js", "common/listing-util.js", "content/panel.js", "content/listing.js"],
```

- [ ] **Step 2: panel.js — 采集按钮渲染**

在 `panel.js` 的 `update()` 中，把 1688 接入真实采集。将：

```js
  function update() {
    if (site === 'ozon') runOzonFollow()
    else if (site === 'wb') renderWbBody()
    else setBody(unsupportedBody())
  }
```

改为：

```js
  function update() {
    if (site === 'ozon') runOzonFollow()
    else if (site === 'wb') renderWbBody()
    else if (site === '1688') render1688Body()
    else setBody(unsupportedBody())
  }

  function render1688Body() {
    if (/\/offer\/\d+\.html/.test(location.pathname)) {
      setBody(actionHtml() + statusHtml('滚动看完详情后再采集，图文更全；采集后进管理端草稿', 'ok'))
    } else {
      setBody(statusHtml('打开 1688 商品详情页后可采集到管理端草稿', 'warn'))
    }
  }
```

- [ ] **Step 3: panel.js — 点击采集路由**

在 `onEditCurrent()` 中，把：

```js
    if (site !== 'ozon') {
      setBody(unsupportedBody())
      return
    }
```

改为：

```js
    if (site === '1688') {
      if (!/\/offer\/\d+\.html/.test(location.pathname)) {
        setBody(statusHtml('打开 1688 商品详情页后可采集', 'warn'))
        return
      }
      OzonHelperCollect.collect1688AndEdit(location.href, setEditButton)
      return
    }
    if (site !== 'ozon') {
      setBody(unsupportedBody())
      return
    }
```

- [ ] **Step 4: 回归测试**

Run: `cd ozon-seller-helper-ext && npx vitest run`
Expected: 全部 PASS。

- [ ] **Step 5: 手动验证（真实插件）**

1. Chrome `chrome://extensions` → 重新加载 `ozon-seller-helper-ext`。
2. 确保已登录 webui 后端（插件 popup 登录），后端可达。
3. 打开任一 1688 商品详情页 `https://detail.1688.com/offer/<id>.html`，**滚动到底**让详情图加载。
4. 右下角浮窗点「采集到管理端草稿」。
5. 期望：状态显示「采集 N/N ✓」；webui 草稿列表出现 N 条草稿（N = SKU 数），同一 `variant_group`；每条有标题、价格、主图、克重尺寸、属性；富文本含详情图（媒体异步传 OSS 后生效）。
6. 抽查一条草稿发布预览，确认 rich content 属性（id=11254）存在且图为 OSS 链接。

- [ ] **Step 6: Commit**

```bash
git add ozon-seller-helper-ext/content/panel.js ozon-seller-helper-ext/manifest.json
git commit -m "feat(ext): panel 路由 1688 采集 + manifest 注册 parse-1688"
```

---

## 验证清单（全部完成后）

- [ ] `cd ozon-seller-helper-ext && npx vitest run` 全绿。
- [ ] 真实 1688 详情页采集成功，webui 出现全量 SKU 草稿。
- [ ] 详情图进入富文本并经 OSS 重托管（发布预览含 id=11254 且图为 OSS 链）。
- [ ] 视频 `video_url` 进草稿并随媒体异步传 OSS。

## 已知实现期注意

- **richAnnotationJson 格式**：Task 3 用 `raShowcase`/`billboard`/`version:0.3`。手动验证(Task 8 Step 6)若 Ozon 不接受，抓一个真实 Ozon A+ 商品的 `richAnnotationJson` 比对后微调 `buildRichContent` 常量（不影响其它任务）。
- **多维度 SKU**：本计划 fixture 为单维度。若遇颜色×尺寸多维商品，`_variantImage` 的 `indexOf(v.name)` 子串匹配可兜底命中第一维图；如需精确，实现期用多维商品验证 `specAttrs` 分隔符后增强。
- **属性容器选择器**：用 `.module-od-product-attributes`；若 1688 改版，回退 `[class*="attribute"]`。
