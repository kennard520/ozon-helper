# 项目约定

## 语言
- **始终用中文回复**（所有解释、总结、状态汇报、提问都用中文）。
- 代码、命令、变量名、提交信息按惯例可保留英文/原文；面向用户的说明文字用中文。

## 执行方式
- 实现计划（superpowers writing-plans 产出的 plan）**一律 subagent 驱动执行**（superpowers:subagent-driven-development）：每个任务派 fresh subagent 实现，任务之间由 controller 独立复核（自己重跑回归/构建/失败数基线，必要时起本地后端做浏览器视觉验收），连续跑完不中途打断。
- writing-plans 完成后**不要再问**“Subagent-Driven 还是 Inline”——直接建任务、派第一个、连续推进。

## 产品文档（每个功能模块一份，改动必须同步）
- **每个功能模块维护一份产品文档**，统一放 `docs/product/<模块名>.md`（如 `workbench.md`、`material-gallery.md`、`pricing.md`、`collect.md`）。
- 文档内容覆盖该模块的：**交互**（用户操作流程/页面与组件/各 tab 与按钮行为）、**数据**（涉及的实体/字段/接口请求与响应形状/状态来源）、**规则**（业务规则、合规校验、边界条件、依赖门控、默认值）。
- **任何改动（新增/修改/删除功能、改交互、改数据结构、改规则）都必须把对应文档一并更新**——和代码改动在同一轮完成，文档是交付物的一部分，不是事后补。改动若跨模块，逐个模块文档都更新。
- 文档面向"接手的人零上下文也能读懂这个模块怎么用、数据怎么流、规则是什么"。用中文写，引用具体文件路径/接口/字段。
- 模块文档与 `docs/superpowers/specs|plans`（一次性设计/实现产物）不同：产品文档是**长期维护的当前事实**，spec/plan 是历史快照。

## 决策与自主（用专业视角评估后直接执行，不必征求意见）
- **后端**的改造/编写：以**架构师 + 资深后端开发**的视角评估（数据模型、性能、N+1、事务、可维护性、规范），定好方案后**直接执行，不需要问我**。
- **前端**的评估/开发：以**产品经理 + 架构师 + 资深前端**的视角评估（用户价值与产品取舍、组件边界与状态模型、可测性、设计系统一致），**直接执行，不需要问我**。
- brainstorm/设计照常做（用上述视角想透 + 写 spec/plan），但**不必停下来等“认可吗”**——给简短结论说明就推进；唯有真正重大/不可逆/破坏性（删数据、对生产库写、改鉴权/权限、对外发布）的决定才先确认。
- 仍要：诚实汇报结果、每任务独立复核（回归/构建/视觉验收）、有疑虑或多个合理方案时给出推荐并说明，而不是反问。
