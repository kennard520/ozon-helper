# Ozon 上品助手 —— 后端运行依赖
# 服务端无需浏览器/Playwright/cloakbrowser。
# 锁定精确版本：在跑通的环境执行 `pip freeze > requirements.lock.txt`。

# --- Web 框架 ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9       # 表单/文件上传

# --- AI ---
openai>=1.30                  # 看图理解 / 文本生成（OpenAI 兼容网关）

# --- 存储 ---
oss2>=2.18                    # 阿里云 OSS 图片直链
PyMySQL>=1.1                  # MySQL 后端（部署到服务器时用；本地 SQLite 不需要）
openpyxl>=3.1                 # 佣金表 Excel 导入/导出（智能定价 realFBS 佣金类目）

# --- 图片 / 消息队列 ---
pillow>=10.0                  # 图片处理与主图/信息图合成
pika>=1.3                     # RabbitMQ 客户端（出图 worker 消息队列）
